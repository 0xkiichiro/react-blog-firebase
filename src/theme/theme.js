import { createTheme } from "@mui/material";

const theme = createTheme({
  //TODO work on to change primary color theme instead!
  colors: {
    navBgColor: "hsl(252, 59%, 52%)",
    bgColor: "hsl(36, 99%, 55%)",
  },
});

export default theme;
